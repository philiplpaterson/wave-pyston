file(COPY ${CMAKE_CURRENT_SOURCE_DIR}/. DESTINATION ${CMAKE_CURRENT_BINARY_DIR})

set(TEST_NAME ${TEST_BASE_NAME}_GaussElim)

add_test(NAME ${TEST_NAME}_preProc COMMAND preProc .)
add_test(NAME ${TEST_NAME}_solver COMMAND solver .)
add_test(NAME ${TEST_NAME}_postProc COMMAND postProc .)

set_tests_properties(${TEST_NAME}_preProc PROPERTIES LABELS "NEMOH1;PREPROC;LIN_SOLVER_TEST")
set_tests_properties(${TEST_NAME}_solver PROPERTIES DEPENDS ${TEST_NAME}_preProc LABELS "NEMOH1;SOLVER;LIN_SOLVER_TEST")
set_tests_properties(${TEST_NAME}_postProc PROPERTIES DEPENDS ${TEST_NAME}_solver LABELS "NEMOH1;POSTPROC;LIN_SOLVER_TEST")
